{
  "schema_version": 1,
  "budgets": [
    {
      "id": "core/render_frame__512",
      "budget_ns": 7957146,
      "tolerance_ratio": 1.2,
      "gate": true,
      "note": "F-074 re-baseline 2026-09-17 (MERGE-IMPL-15): captured on the same machine under rustc 1.98.0 against median_ns = 3978573 ns, budget_ns = 2x. tolerance_ratio 1.2 absorbs same-machine run noise. CI bench job runs compare.mjs in report/warn only (never this hard gate); gate:true enables a controlled local regression gate on the capture machine."
    },
    {
      "id": "core/render_frame__1024",
      "budget_ns": 32716028,
      "tolerance_ratio": 1.2,
      "gate": true,
      "note": "F-074 re-baseline 2026-09-17 (MERGE-IMPL-15): captured on the same machine under rustc 1.98.0 against median_ns = 16358014 ns, budget_ns = 2x. tolerance_ratio 1.2 absorbs same-machine run noise. CI bench job runs compare.mjs in report/warn only (never this hard gate); gate:true enables a controlled local regression gate on the capture machine."
    },
    {
      "id": "core/render_frame__2048",
      "budget_ns": 131516708,
      "tolerance_ratio": 1.2,
      "gate": true,
      "note": "F-074 re-baseline 2026-09-17 (MERGE-IMPL-15): captured on the same machine under rustc 1.98.0 against median_ns = 65758354 ns, budget_ns = 2x. tolerance_ratio 1.2 absorbs same-machine run noise. CI bench job runs compare.mjs in report/warn only (never this hard gate); gate:true enables a controlled local regression gate on the capture machine."
    },
    {
      "id": "core/apply_recipe_with_white_balance__512",
      "budget_ns": 6777176,
      "tolerance_ratio": 1.2,
      "gate": true,
      "note": "F-074 re-baseline 2026-09-17 (MERGE-IMPL-15): captured on the same machine under rustc 1.98.0 against median_ns = 3388588 ns, budget_ns = 2x. tolerance_ratio 1.2 absorbs same-machine run noise. CI bench job runs compare.mjs in report/warn only (never this hard gate); gate:true enables a controlled local regression gate on the capture machine."
    },
    {
      "id": "core/apply_recipe_with_white_balance__1024",
      "budget_ns": 28045236,
      "tolerance_ratio": 1.2,
      "gate": true,
      "note": "F-074 re-baseline 2026-09-17 (MERGE-IMPL-15): captured on the same machine under rustc 1.98.0 against median_ns = 14022618 ns, budget_ns = 2x. tolerance_ratio 1.2 absorbs same-machine run noise. CI bench job runs compare.mjs in report/warn only (never this hard gate); gate:true enables a controlled local regression gate on the capture machine."
    },
    {
      "id": "core/apply_recipe_with_white_balance__2048",
      "budget_ns": 112783084,
      "tolerance_ratio": 1.2,
      "gate": true,
      "note": "F-074 re-baseline 2026-09-17 (MERGE-IMPL-15): captured on the same machine under rustc 1.98.0 against median_ns = 56391542 ns, budget_ns = 2x. tolerance_ratio 1.2 absorbs same-machine run noise. CI bench job runs compare.mjs in report/warn only (never this hard gate); gate:true enables a controlled local regression gate on the capture machine."
    },
    {
      "id": "core/mask_graph_eval__512",
      "budget_ns": 62618,
      "tolerance_ratio": 1.2,
      "gate": true,
      "note": "F-074 re-baseline 2026-09-17 (MERGE-IMPL-15): captured on the same machine under rustc 1.98.0 against median_ns = 31309 ns, budget_ns = 2x. tolerance_ratio 1.2 absorbs same-machine run noise. CI bench job runs compare.mjs in report/warn only (never this hard gate); gate:true enables a controlled local regression gate on the capture machine."
    },
    {
      "id": "core/mask_graph_eval__1024",
      "budget_ns": 103746,
      "tolerance_ratio": 1.2,
      "gate": true,
      "note": "F-074 re-baseline 2026-09-17 (MERGE-IMPL-15): captured on the same machine under rustc 1.98.0 against median_ns = 51873 ns, budget_ns = 2x. tolerance_ratio 1.2 absorbs same-machine run noise. CI bench job runs compare.mjs in report/warn only (never this hard gate); gate:true enables a controlled local regression gate on the capture machine."
    },
    {
      "id": "core/mask_graph_eval__2048",
      "budget_ns": 448514,
      "tolerance_ratio": 1.2,
      "gate": true,
      "note": "F-074 re-baseline 2026-09-17 (MERGE-IMPL-15): captured on the same machine under rustc 1.98.0 against median_ns = 224257 ns, budget_ns = 2x. tolerance_ratio 1.2 absorbs same-machine run noise. CI bench job runs compare.mjs in report/warn only (never this hard gate); gate:true enables a controlled local regression gate on the capture machine."
    },
    {
      "id": "core/analyze_tone__512",
      "budget_ns": 374342,
      "tolerance_ratio": 1.2,
      "gate": true,
      "note": "F-074 re-baseline 2026-09-17 (MERGE-IMPL-15): captured on the same machine under rustc 1.98.0 against median_ns = 187171 ns, budget_ns = 2x. tolerance_ratio 1.2 absorbs same-machine run noise. CI bench job runs compare.mjs in report/warn only (never this hard gate); gate:true enables a controlled local regression gate on the capture machine."
    },
    {
      "id": "core/analyze_tone__1024",
      "budget_ns": 1498130,
      "tolerance_ratio": 1.2,
      "gate": true,
      "note": "F-074 re-baseline 2026-09-17 (MERGE-IMPL-15): captured on the same machine under rustc 1.98.0 against median_ns = 749065 ns, budget_ns = 2x. tolerance_ratio 1.2 absorbs same-machine run noise. CI bench job runs compare.mjs in report/warn only (never this hard gate); gate:true enables a controlled local regression gate on the capture machine."
    },
    {
      "id": "core/analyze_tone__2048",
      "budget_ns": 6000496,
      "tolerance_ratio": 1.2,
      "gate": true,
      "note": "F-074 re-baseline 2026-09-17 (MERGE-IMPL-15): captured on the same machine under rustc 1.98.0 against median_ns = 3000248 ns, budget_ns = 2x. tolerance_ratio 1.2 absorbs same-machine run noise. CI bench job runs compare.mjs in report/warn only (never this hard gate); gate:true enables a controlled local regression gate on the capture machine."
    },
    {
      "id": "core/suggest_auto_tone__512",
      "budget_ns": 374462,
      "tolerance_ratio": 1.2,
      "gate": true,
      "note": "F-074 re-baseline 2026-09-17 (MERGE-IMPL-15): captured on the same machine under rustc 1.98.0 against median_ns = 187231 ns, budget_ns = 2x. tolerance_ratio 1.2 absorbs same-machine run noise. CI bench job runs compare.mjs in report/warn only (never this hard gate); gate:true enables a controlled local regression gate on the capture machine."
    },
    {
      "id": "core/suggest_auto_tone__1024",
      "budget_ns": 1499302,
      "tolerance_ratio": 1.2,
      "gate": true,
      "note": "F-074 re-baseline 2026-09-17 (MERGE-IMPL-15): captured on the same machine under rustc 1.98.0 against median_ns = 749651 ns, budget_ns = 2x. tolerance_ratio 1.2 absorbs same-machine run noise. CI bench job runs compare.mjs in report/warn only (never this hard gate); gate:true enables a controlled local regression gate on the capture machine."
    },
    {
      "id": "core/suggest_auto_tone__2048",
      "budget_ns": 6002390,
      "tolerance_ratio": 1.2,
      "gate": true,
      "note": "F-074 re-baseline 2026-09-17 (MERGE-IMPL-15): captured on the same machine under rustc 1.98.0 against median_ns = 3001195 ns, budget_ns = 2x. tolerance_ratio 1.2 absorbs same-machine run noise. CI bench job runs compare.mjs in report/warn only (never this hard gate); gate:true enables a controlled local regression gate on the capture machine."
    },
    {
      "id": "core/match_total_exposure__512",
      "budget_ns": 382798,
      "tolerance_ratio": 1.2,
      "gate": true,
      "note": "F-074 re-baseline 2026-09-17 (MERGE-IMPL-15): captured on the same machine under rustc 1.98.0 against median_ns = 191399 ns, budget_ns = 2x. tolerance_ratio 1.2 absorbs same-machine run noise. CI bench job runs compare.mjs in report/warn only (never this hard gate); gate:true enables a controlled local regression gate on the capture machine."
    },
    {
      "id": "core/match_total_exposure__1024",
      "budget_ns": 1527606,
      "tolerance_ratio": 1.2,
      "gate": true,
      "note": "F-074 re-baseline 2026-09-17 (MERGE-IMPL-15): captured on the same machine under rustc 1.98.0 against median_ns = 763803 ns, budget_ns = 2x. tolerance_ratio 1.2 absorbs same-machine run noise. CI bench job runs compare.mjs in report/warn only (never this hard gate); gate:true enables a controlled local regression gate on the capture machine."
    },
    {
      "id": "core/match_total_exposure__2048",
      "budget_ns": 6123642,
      "tolerance_ratio": 1.2,
      "gate": true,
      "note": "F-074 re-baseline 2026-09-17 (MERGE-IMPL-15): captured on the same machine under rustc 1.98.0 against median_ns = 3061821 ns, budget_ns = 2x. tolerance_ratio 1.2 absorbs same-machine run noise. CI bench job runs compare.mjs in report/warn only (never this hard gate); gate:true enables a controlled local regression gate on the capture machine."
    },
    {
      "id": "core/histogram__512",
      "budget_ns": 357180,
      "tolerance_ratio": 1.2,
      "gate": true,
      "note": "F-074 re-baseline 2026-09-17 (MERGE-IMPL-15): captured on the same machine under rustc 1.98.0 against median_ns = 178590 ns, budget_ns = 2x. tolerance_ratio 1.2 absorbs same-machine run noise. CI bench job runs compare.mjs in report/warn only (never this hard gate); gate:true enables a controlled local regression gate on the capture machine."
    },
    {
      "id": "core/histogram__1024",
      "budget_ns": 1432730,
      "tolerance_ratio": 1.2,
      "gate": true,
      "note": "F-074 re-baseline 2026-09-17 (MERGE-IMPL-15): captured on the same machine under rustc 1.98.0 against median_ns = 716365 ns, budget_ns = 2x. tolerance_ratio 1.2 absorbs same-machine run noise. CI bench job runs compare.mjs in report/warn only (never this hard gate); gate:true enables a controlled local regression gate on the capture machine."
    },
    {
      "id": "core/histogram__2048",
      "budget_ns": 5739896,
      "tolerance_ratio": 1.2,
      "gate": true,
      "note": "F-074 re-baseline 2026-09-17 (MERGE-IMPL-15): captured on the same machine under rustc 1.98.0 against median_ns = 2869948 ns, budget_ns = 2x. tolerance_ratio 1.2 absorbs same-machine run noise. CI bench job runs compare.mjs in report/warn only (never this hard gate); gate:true enables a controlled local regression gate on the capture machine."
    },
    {
      "id": "core/cache_hit__512",
      "budget_ns": 16,
      "tolerance_ratio": 1.2,
      "gate": true,
      "note": "F-074 re-baseline 2026-09-17 (MERGE-IMPL-15): captured on the same machine under rustc 1.98.0 against median_ns = 8 ns, budget_ns = 2x. tolerance_ratio 1.2 absorbs same-machine run noise. CI bench job runs compare.mjs in report/warn only (never this hard gate); gate:true enables a controlled local regression gate on the capture machine."
    },
    {
      "id": "core/cache_hit__1024",
      "budget_ns": 16,
      "tolerance_ratio": 1.2,
      "gate": true,
      "note": "F-074 re-baseline 2026-09-17 (MERGE-IMPL-15): captured on the same machine under rustc 1.98.0 against median_ns = 8 ns, budget_ns = 2x. tolerance_ratio 1.2 absorbs same-machine run noise. CI bench job runs compare.mjs in report/warn only (never this hard gate); gate:true enables a controlled local regression gate on the capture machine."
    },
    {
      "id": "core/cache_hit__2048",
      "budget_ns": 18,
      "tolerance_ratio": 1.2,
      "gate": true,
      "note": "F-074 re-baseline 2026-09-17 (MERGE-IMPL-15): captured on the same machine under rustc 1.98.0 against median_ns = 9 ns, budget_ns = 2x. tolerance_ratio 1.2 absorbs same-machine run noise. CI bench job runs compare.mjs in report/warn only (never this hard gate); gate:true enables a controlled local regression gate on the capture machine."
    },
    {
      "id": "core/cache_miss__512",
      "budget_ns": 12,
      "tolerance_ratio": 1.2,
      "gate": true,
      "note": "F-074 re-baseline 2026-09-17 (MERGE-IMPL-15): captured on the same machine under rustc 1.98.0 against median_ns = 6 ns, budget_ns = 2x. tolerance_ratio 1.2 absorbs same-machine run noise. CI bench job runs compare.mjs in report/warn only (never this hard gate); gate:true enables a controlled local regression gate on the capture machine."
    },
    {
      "id": "core/cache_miss__1024",
      "budget_ns": 12,
      "tolerance_ratio": 1.2,
      "gate": true,
      "note": "F-074 re-baseline 2026-09-17 (MERGE-IMPL-15): captured on the same machine under rustc 1.98.0 against median_ns = 6 ns, budget_ns = 2x. tolerance_ratio 1.2 absorbs same-machine run noise. CI bench job runs compare.mjs in report/warn only (never this hard gate); gate:true enables a controlled local regression gate on the capture machine."
    },
    {
      "id": "core/cache_miss__2048",
      "budget_ns": 12,
      "tolerance_ratio": 1.2,
      "gate": true,
      "note": "F-074 re-baseline 2026-09-17 (MERGE-IMPL-15): captured on the same machine under rustc 1.98.0 against median_ns = 6 ns, budget_ns = 2x. tolerance_ratio 1.2 absorbs same-machine run noise. CI bench job runs compare.mjs in report/warn only (never this hard gate); gate:true enables a controlled local regression gate on the capture machine."
    },
    {
      "id": "batch/render_export_png__512",
      "budget_ns": 12230538,
      "tolerance_ratio": 1.2,
      "gate": true,
      "note": "F-074 re-baseline 2026-09-17 (MERGE-IMPL-15): captured on the same machine under rustc 1.98.0 against median_ns = 6115269 ns, budget_ns = 2x. tolerance_ratio 1.2 absorbs same-machine run noise. CI bench job runs compare.mjs in report/warn only (never this hard gate); gate:true enables a controlled local regression gate on the capture machine."
    },
    {
      "id": "batch/render_export_png__1024",
      "budget_ns": 50967458,
      "tolerance_ratio": 1.2,
      "gate": true,
      "note": "F-074 re-baseline 2026-09-17 (MERGE-IMPL-15): captured on the same machine under rustc 1.98.0 against median_ns = 25483729 ns, budget_ns = 2x. tolerance_ratio 1.2 absorbs same-machine run noise. CI bench job runs compare.mjs in report/warn only (never this hard gate); gate:true enables a controlled local regression gate on the capture machine."
    },
    {
      "id": "batch/render_export_png__2048",
      "budget_ns": 207322376,
      "tolerance_ratio": 1.2,
      "gate": true,
      "note": "F-074 re-baseline 2026-09-17 (MERGE-IMPL-15): captured on the same machine under rustc 1.98.0 against median_ns = 103661188 ns, budget_ns = 2x. tolerance_ratio 1.2 absorbs same-machine run noise. CI bench job runs compare.mjs in report/warn only (never this hard gate); gate:true enables a controlled local regression gate on the capture machine."
    },
    {
      "id": "decode/raw__aircraft-landscape",
      "budget_ns": 643784500,
      "tolerance_ratio": 1.2,
      "gate": false,
      "note": "Env-gated decode benchmark; non-deterministic across machines and CI runners (LibRaw decode + fixture I/O, thermals). Kept gate:false to avoid false regressions on noisy runners; decode regression is tracked via report/warn only (see F-074-A2)."
    },
    {
      "id": "decode/raw__aircraft-portrait",
      "budget_ns": 721075666,
      "tolerance_ratio": 1.2,
      "gate": false,
      "note": "Env-gated decode benchmark; non-deterministic across machines and CI runners (LibRaw decode + fixture I/O, thermals). Kept gate:false to avoid false regressions on noisy runners; decode regression is tracked via report/warn only (see F-074-A2)."
    },
    {
      "id": "gpu/render_with_gpu__512",
      "budget_ns": 514614,
      "tolerance_ratio": 1.2,
      "gate": true,
      "note": "F-074 re-baseline 2026-09-17 (MERGE-IMPL-15): GPU path recaptured on the capture machine (rustc 1.98.0) against median_ns = 257307 ns, budget_ns = 2x. tolerance_ratio 1.2 absorbs same-machine run noise. CI bench job runs compare.mjs in report/warn only (never this hard gate); gate:true enables a controlled local regression gate on the capture machine."
    },
    {
      "id": "gpu/render_with_gpu__1024",
      "budget_ns": 708326,
      "tolerance_ratio": 1.2,
      "gate": true,
      "note": "F-074 re-baseline 2026-09-17 (MERGE-IMPL-15): GPU path recaptured on the capture machine (rustc 1.98.0) against median_ns = 354163 ns, budget_ns = 2x. tolerance_ratio 1.2 absorbs same-machine run noise. CI bench job runs compare.mjs in report/warn only (never this hard gate); gate:true enables a controlled local regression gate on the capture machine."
    },
    {
      "id": "gpu/render_with_gpu__2048",
      "budget_ns": 2152540,
      "tolerance_ratio": 1.2,
      "gate": true,
      "note": "F-074 re-baseline 2026-09-17 (MERGE-IMPL-15): GPU path recaptured on the capture machine (rustc 1.98.0) against median_ns = 1076270 ns, budget_ns = 2x. tolerance_ratio 1.2 absorbs same-machine run noise. CI bench job runs compare.mjs in report/warn only (never this hard gate); gate:true enables a controlled local regression gate on the capture machine."
    },
    {
      "id": "gpu/update_uniforms__recipe",
      "budget_ns": 27026,
      "tolerance_ratio": 1.2,
      "gate": true,
      "note": "F-074 re-baseline 2026-09-17 (MERGE-IMPL-15): GPU path recaptured on the capture machine (rustc 1.98.0) against median_ns = 13513 ns, budget_ns = 2x. tolerance_ratio 1.2 absorbs same-machine run noise. CI bench job runs compare.mjs in report/warn only (never this hard gate); gate:true enables a controlled local regression gate on the capture machine."
    },
    {
      "id": "gpu/cpu_vs_gpu__cpu__2048",
      "budget_ns": 110987168,
      "tolerance_ratio": 1.2,
      "gate": true,
      "note": "F-074 re-baseline 2026-09-17 (MERGE-IMPL-15): GPU path recaptured on the capture machine (rustc 1.98.0) against median_ns = 55493584 ns, budget_ns = 2x. tolerance_ratio 1.2 absorbs same-machine run noise. CI bench job runs compare.mjs in report/warn only (never this hard gate); gate:true enables a controlled local regression gate on the capture machine."
    },
    {
      "id": "gpu/cpu_vs_gpu__gpu__2048",
      "budget_ns": 2212338,
      "tolerance_ratio": 1.2,
      "gate": true,
      "note": "F-074 re-baseline 2026-09-17 (MERGE-IMPL-15): GPU path recaptured on the capture machine (rustc 1.98.0) against median_ns = 1106169 ns, budget_ns = 2x. tolerance_ratio 1.2 absorbs same-machine run noise. CI bench job runs compare.mjs in report/warn only (never this hard gate); gate:true enables a controlled local regression gate on the capture machine."
    },
    {
      "id": "merge/hdr_weighted__512",
      "budget_ns": 13867902,
      "tolerance_ratio": 1.2,
      "gate": false,
      "note": "New merge class (LRPAR-G13-MERGE-15 / MERGE-IMPL-15), measured with `cargo bench -p lumina-bench --bench merge`. Captured 2026-09-17 on the capture machine (rustc 1.98.0) against baseline median_ns = 6933951 ns, budget_ns = 2x. gate:false (report-only) until the class is independently calibrated (F-074 methodology: new benchmarks start in report mode, like the GPU class). tolerance_ratio 1.2 absorbs same-machine run noise. Deterministic synthetic fixtures (seed regime of bench/common/mod.rs), no network. merge/align_hdr__512 uses max_shift_px = 8 (exhaustive SAD search radius, documented in bench/merge.rs)."
    },
    {
      "id": "merge/hdr_weighted__1024",
      "budget_ns": 55998194,
      "tolerance_ratio": 1.2,
      "gate": false,
      "note": "New merge class (LRPAR-G13-MERGE-15 / MERGE-IMPL-15), measured with `cargo bench -p lumina-bench --bench merge`. Captured 2026-09-17 on the capture machine (rustc 1.98.0) against baseline median_ns = 27999097 ns, budget_ns = 2x. gate:false (report-only) until the class is independently calibrated (F-074 methodology: new benchmarks start in report mode, like the GPU class). tolerance_ratio 1.2 absorbs same-machine run noise. Deterministic synthetic fixtures (seed regime of bench/common/mod.rs), no network. merge/align_hdr__512 uses max_shift_px = 8 (exhaustive SAD search radius, documented in bench/merge.rs)."
    },
    {
      "id": "merge/hdr_weighted__2048",
      "budget_ns": 226733624,
      "tolerance_ratio": 1.2,
      "gate": false,
      "note": "New merge class (LRPAR-G13-MERGE-15 / MERGE-IMPL-15), measured with `cargo bench -p lumina-bench --bench merge`. Captured 2026-09-17 on the capture machine (rustc 1.98.0) against baseline median_ns = 113366812 ns, budget_ns = 2x. gate:false (report-only) until the class is independently calibrated (F-074 methodology: new benchmarks start in report mode, like the GPU class). tolerance_ratio 1.2 absorbs same-machine run noise. Deterministic synthetic fixtures (seed regime of bench/common/mod.rs), no network. merge/align_hdr__512 uses max_shift_px = 8 (exhaustive SAD search radius, documented in bench/merge.rs)."
    },
    {
      "id": "merge/pano_blend__512",
      "budget_ns": 15649754,
      "tolerance_ratio": 1.2,
      "gate": false,
      "note": "New merge class (LRPAR-G13-MERGE-15 / MERGE-IMPL-15), measured with `cargo bench -p lumina-bench --bench merge`. Captured 2026-09-17 on the capture machine (rustc 1.98.0) against baseline median_ns = 7824877 ns, budget_ns = 2x. gate:false (report-only) until the class is independently calibrated (F-074 methodology: new benchmarks start in report mode, like the GPU class). tolerance_ratio 1.2 absorbs same-machine run noise. Deterministic synthetic fixtures (seed regime of bench/common/mod.rs), no network. merge/align_hdr__512 uses max_shift_px = 8 (exhaustive SAD search radius, documented in bench/merge.rs)."
    },
    {
      "id": "merge/pano_blend__1024",
      "budget_ns": 64397320,
      "tolerance_ratio": 1.2,
      "gate": false,
      "note": "New merge class (LRPAR-G13-MERGE-15 / MERGE-IMPL-15), measured with `cargo bench -p lumina-bench --bench merge`. Captured 2026-09-17 on the capture machine (rustc 1.98.0) against baseline median_ns = 32198660 ns, budget_ns = 2x. gate:false (report-only) until the class is independently calibrated (F-074 methodology: new benchmarks start in report mode, like the GPU class). tolerance_ratio 1.2 absorbs same-machine run noise. Deterministic synthetic fixtures (seed regime of bench/common/mod.rs), no network. merge/align_hdr__512 uses max_shift_px = 8 (exhaustive SAD search radius, documented in bench/merge.rs)."
    },
    {
      "id": "merge/pano_blend__2048",
      "budget_ns": 254680708,
      "tolerance_ratio": 1.2,
      "gate": false,
      "note": "New merge class (LRPAR-G13-MERGE-15 / MERGE-IMPL-15), measured with `cargo bench -p lumina-bench --bench merge`. Captured 2026-09-17 on the capture machine (rustc 1.98.0) against baseline median_ns = 127340354 ns, budget_ns = 2x. gate:false (report-only) until the class is independently calibrated (F-074 methodology: new benchmarks start in report mode, like the GPU class). tolerance_ratio 1.2 absorbs same-machine run noise. Deterministic synthetic fixtures (seed regime of bench/common/mod.rs), no network. merge/align_hdr__512 uses max_shift_px = 8 (exhaustive SAD search radius, documented in bench/merge.rs)."
    },
    {
      "id": "merge/encode_dng__512",
      "budget_ns": 391834,
      "tolerance_ratio": 1.2,
      "gate": false,
      "note": "New merge class (LRPAR-G13-MERGE-15 / MERGE-IMPL-15), measured with `cargo bench -p lumina-bench --bench merge`. Captured 2026-09-17 on the capture machine (rustc 1.98.0) against baseline median_ns = 195917 ns, budget_ns = 2x. gate:false (report-only) until the class is independently calibrated (F-074 methodology: new benchmarks start in report mode, like the GPU class). tolerance_ratio 1.2 absorbs same-machine run noise. Deterministic synthetic fixtures (seed regime of bench/common/mod.rs), no network. merge/align_hdr__512 uses max_shift_px = 8 (exhaustive SAD search radius, documented in bench/merge.rs)."
    },
    {
      "id": "merge/encode_dng__1024",
      "budget_ns": 1401470,
      "tolerance_ratio": 1.2,
      "gate": false,
      "note": "New merge class (LRPAR-G13-MERGE-15 / MERGE-IMPL-15), measured with `cargo bench -p lumina-bench --bench merge`. Captured 2026-09-17 on the capture machine (rustc 1.98.0) against baseline median_ns = 700735 ns, budget_ns = 2x. gate:false (report-only) until the class is independently calibrated (F-074 methodology: new benchmarks start in report mode, like the GPU class). tolerance_ratio 1.2 absorbs same-machine run noise. Deterministic synthetic fixtures (seed regime of bench/common/mod.rs), no network. merge/align_hdr__512 uses max_shift_px = 8 (exhaustive SAD search radius, documented in bench/merge.rs)."
    },
    {
      "id": "merge/encode_dng__2048",
      "budget_ns": 5828716,
      "tolerance_ratio": 1.2,
      "gate": false,
      "note": "New merge class (LRPAR-G13-MERGE-15 / MERGE-IMPL-15), measured with `cargo bench -p lumina-bench --bench merge`. Captured 2026-09-17 on the capture machine (rustc 1.98.0) against baseline median_ns = 2914358 ns, budget_ns = 2x. gate:false (report-only) until the class is independently calibrated (F-074 methodology: new benchmarks start in report mode, like the GPU class). tolerance_ratio 1.2 absorbs same-machine run noise. Deterministic synthetic fixtures (seed regime of bench/common/mod.rs), no network. merge/align_hdr__512 uses max_shift_px = 8 (exhaustive SAD search radius, documented in bench/merge.rs)."
    },
    {
      "id": "merge/align_hdr__512",
      "budget_ns": 450222044,
      "tolerance_ratio": 1.2,
      "gate": false,
      "note": "New merge class (LRPAR-G13-MERGE-15 / MERGE-IMPL-15), measured with `cargo bench -p lumina-bench --bench merge`. Captured 2026-09-17 on the capture machine (rustc 1.98.0) against baseline median_ns = 225111022 ns, budget_ns = 2x. gate:false (report-only) until the class is independently calibrated (F-074 methodology: new benchmarks start in report mode, like the GPU class). tolerance_ratio 1.2 absorbs same-machine run noise. Deterministic synthetic fixtures (seed regime of bench/common/mod.rs), no network. merge/align_hdr__512 uses max_shift_px = 8 (exhaustive SAD search radius, documented in bench/merge.rs)."
    },
    {
      "id": "denoise/blend__512",
      "budget_ns": 2983396,
      "tolerance_ratio": 1.2,
      "gate": false,
      "note": "New denoise class (LRPAR-G14-DENOISE-IMPL-20 / F-074), measured with `cargo bench -p lumina-bench --bench denoise`. Captured 2026-09-17 on the capture machine (rustc 1.98.0) against baseline median_ns = 1491698 ns, budget_ns = 2x. gate:false (report-only) until the class is independently calibrated (F-074 methodology: new classes start in report mode, like the GPU/Merge classes). Deterministic synthetic fixtures (seed regime of bench/common/mod.rs), no network. NOT a model benchmark: ONNX weights are `pending-integration` (decision §3.1), so this measures the model-free fixture path only, never denoiser throughput."
    },
    {
      "id": "denoise/blend__1024",
      "budget_ns": 11963172,
      "tolerance_ratio": 1.2,
      "gate": false,
      "note": "New denoise class (LRPAR-G14-DENOISE-IMPL-20 / F-074), measured with `cargo bench -p lumina-bench --bench denoise`. Captured 2026-09-17 on the capture machine (rustc 1.98.0) against baseline median_ns = 5981586 ns, budget_ns = 2x. gate:false (report-only) until the class is independently calibrated (F-074 methodology: new classes start in report mode, like the GPU/Merge classes). Deterministic synthetic fixtures (seed regime of bench/common/mod.rs), no network. NOT a model benchmark: ONNX weights are `pending-integration` (decision §3.1), so this measures the model-free fixture path only, never denoiser throughput."
    },
    {
      "id": "denoise/blend__2048",
      "budget_ns": 48180354,
      "tolerance_ratio": 1.2,
      "gate": false,
      "note": "New denoise class (LRPAR-G14-DENOISE-IMPL-20 / F-074), measured with `cargo bench -p lumina-bench --bench denoise`. Captured 2026-09-17 on the capture machine (rustc 1.98.0) against baseline median_ns = 24090177 ns, budget_ns = 2x. gate:false (report-only) until the class is independently calibrated (F-074 methodology: new classes start in report mode, like the GPU/Merge classes). Deterministic synthetic fixtures (seed regime of bench/common/mod.rs), no network. NOT a model benchmark: ONNX weights are `pending-integration` (decision §3.1), so this measures the model-free fixture path only, never denoiser throughput."
    },
    {
      "id": "denoise/assemble_tiles__512",
      "budget_ns": 1559964,
      "tolerance_ratio": 1.2,
      "gate": false,
      "note": "New denoise class (LRPAR-G14-DENOISE-IMPL-20 / F-074), measured with `cargo bench -p lumina-bench --bench denoise`. Captured 2026-09-17 on the capture machine (rustc 1.98.0) against baseline median_ns = 779982 ns, budget_ns = 2x. gate:false (report-only) until the class is independently calibrated (F-074 methodology: new classes start in report mode, like the GPU/Merge classes). Deterministic synthetic fixtures (seed regime of bench/common/mod.rs), no network. NOT a model benchmark: ONNX weights are `pending-integration` (decision §3.1), so this measures the model-free fixture path only, never denoiser throughput."
    },
    {
      "id": "denoise/assemble_tiles__1024",
      "budget_ns": 9970732,
      "tolerance_ratio": 1.2,
      "gate": false,
      "note": "New denoise class (LRPAR-G14-DENOISE-IMPL-20 / F-074), measured with `cargo bench -p lumina-bench --bench denoise`. Captured 2026-09-17 on the capture machine (rustc 1.98.0) against baseline median_ns = 4985366 ns, budget_ns = 2x. gate:false (report-only) until the class is independently calibrated (F-074 methodology: new classes start in report mode, like the GPU/Merge classes). Deterministic synthetic fixtures (seed regime of bench/common/mod.rs), no network. NOT a model benchmark: ONNX weights are `pending-integration` (decision §3.1), so this measures the model-free fixture path only, never denoiser throughput."
    },
    {
      "id": "denoise/assemble_tiles__2048",
      "budget_ns": 33697664,
      "tolerance_ratio": 1.2,
      "gate": false,
      "note": "New denoise class (LRPAR-G14-DENOISE-IMPL-20 / F-074), measured with `cargo bench -p lumina-bench --bench denoise`. Captured 2026-09-17 on the capture machine (rustc 1.98.0) against baseline median_ns = 16848832 ns, budget_ns = 2x. gate:false (report-only) until the class is independently calibrated (F-074 methodology: new classes start in report mode, like the GPU/Merge classes). Deterministic synthetic fixtures (seed regime of bench/common/mod.rs), no network. NOT a model benchmark: ONNX weights are `pending-integration` (decision §3.1), so this measures the model-free fixture path only, never denoiser throughput."
    },
    {
      "id": "denoise/render_ready__512",
      "budget_ns": 9382716,
      "tolerance_ratio": 1.2,
      "gate": false,
      "note": "New denoise class (LRPAR-G14-DENOISE-IMPL-20 / F-074), measured with `cargo bench -p lumina-bench --bench denoise`. Captured 2026-09-17 on the capture machine (rustc 1.98.0) against baseline median_ns = 4691358 ns, budget_ns = 2x. gate:false (report-only) until the class is independently calibrated (F-074 methodology: new classes start in report mode, like the GPU/Merge classes). Deterministic synthetic fixtures (seed regime of bench/common/mod.rs), no network. NOT a model benchmark: ONNX weights are `pending-integration` (decision §3.1), so this measures the model-free fixture path only, never denoiser throughput."
    },
    {
      "id": "denoise/render_ready__1024",
      "budget_ns": 39353232,
      "tolerance_ratio": 1.2,
      "gate": false,
      "note": "New denoise class (LRPAR-G14-DENOISE-IMPL-20 / F-074), measured with `cargo bench -p lumina-bench --bench denoise`. Captured 2026-09-17 on the capture machine (rustc 1.98.0) against baseline median_ns = 19676616 ns, budget_ns = 2x. gate:false (report-only) until the class is independently calibrated (F-074 methodology: new classes start in report mode, like the GPU/Merge classes). Deterministic synthetic fixtures (seed regime of bench/common/mod.rs), no network. NOT a model benchmark: ONNX weights are `pending-integration` (decision §3.1), so this measures the model-free fixture path only, never denoiser throughput."
    },
    {
      "id": "denoise/render_ready__2048",
      "budget_ns": 160542688,
      "tolerance_ratio": 1.2,
      "gate": false,
      "note": "New denoise class (LRPAR-G14-DENOISE-IMPL-20 / F-074), measured with `cargo bench -p lumina-bench --bench denoise`. Captured 2026-09-17 on the capture machine (rustc 1.98.0) against baseline median_ns = 80271344 ns, budget_ns = 2x. gate:false (report-only) until the class is independently calibrated (F-074 methodology: new classes start in report mode, like the GPU/Merge classes). Deterministic synthetic fixtures (seed regime of bench/common/mod.rs), no network. NOT a model benchmark: ONNX weights are `pending-integration` (decision §3.1), so this measures the model-free fixture path only, never denoiser throughput."
    },
    {
      "id": "denoise/status_resolve__ready",
      "budget_ns": 42,
      "tolerance_ratio": 1.2,
      "gate": false,
      "note": "New denoise class (LRPAR-G14-DENOISE-IMPL-20 / F-074), measured with `cargo bench -p lumina-bench --bench denoise`. Captured 2026-09-17 on the capture machine (rustc 1.98.0) against baseline median_ns = 21 ns, budget_ns = 2x. gate:false (report-only) until the class is independently calibrated (F-074 methodology: new classes start in report mode, like the GPU/Merge classes). Deterministic synthetic fixtures (seed regime of bench/common/mod.rs), no network. NOT a model benchmark: ONNX weights are `pending-integration` (decision §3.1), so this measures the model-free fixture path only, never denoiser throughput."
    },
    {
      "id": "cull/analyze__512",
      "budget_ns": 4095692,
      "tolerance_ratio": 1.2,
      "gate": false,
      "note": "New assisted-culling class (LRPAR-G09-CULL-25 / F-074), measured with `cargo bench -p lumina-bench --bench cull`. Captured 2026-09-17 on the capture machine (rustc 1.98.0) against baseline median_ns = 2047846 ns, budget_ns = 2x. gate:false (report-only) until the class is independently calibrated (F-074 methodology: new classes start in report mode, like the GPU/Merge classes). Deterministic, model-free Stage-1 heuristic, no ONNX/weights, no network (seed regime of bench/common/mod.rs). The `cull/noise_sigma__*` kernel carries the ~22 MB transient-allocation hotspot noted in the decision doc §9."
    },
    {
      "id": "cull/analyze__1024",
      "budget_ns": 16736114,
      "tolerance_ratio": 1.2,
      "gate": false,
      "note": "New assisted-culling class (LRPAR-G09-CULL-25 / F-074), measured with `cargo bench -p lumina-bench --bench cull`. Captured 2026-09-17 on the capture machine (rustc 1.98.0) against baseline median_ns = 8368057 ns, budget_ns = 2x. gate:false (report-only) until the class is independently calibrated (F-074 methodology: new classes start in report mode, like the GPU/Merge classes). Deterministic, model-free Stage-1 heuristic, no ONNX/weights, no network (seed regime of bench/common/mod.rs). The `cull/noise_sigma__*` kernel carries the ~22 MB transient-allocation hotspot noted in the decision doc §9."
    },
    {
      "id": "cull/analyze__2048",
      "budget_ns": 67522136,
      "tolerance_ratio": 1.2,
      "gate": false,
      "note": "New assisted-culling class (LRPAR-G09-CULL-25 / F-074), measured with `cargo bench -p lumina-bench --bench cull`. Captured 2026-09-17 on the capture machine (rustc 1.98.0) against baseline median_ns = 33761068 ns, budget_ns = 2x. gate:false (report-only) until the class is independently calibrated (F-074 methodology: new classes start in report mode, like the GPU/Merge classes). Deterministic, model-free Stage-1 heuristic, no ONNX/weights, no network (seed regime of bench/common/mod.rs). The `cull/noise_sigma__*` kernel carries the ~22 MB transient-allocation hotspot noted in the decision doc §9."
    },
    {
      "id": "cull/noise_sigma__512",
      "budget_ns": 1791872,
      "tolerance_ratio": 1.2,
      "gate": false,
      "note": "New assisted-culling class (LRPAR-G09-CULL-25 / F-074), measured with `cargo bench -p lumina-bench --bench cull`. Captured 2026-09-17 on the capture machine (rustc 1.98.0) against baseline median_ns = 895936 ns, budget_ns = 2x. gate:false (report-only) until the class is independently calibrated (F-074 methodology: new classes start in report mode, like the GPU/Merge classes). Deterministic, model-free Stage-1 heuristic, no ONNX/weights, no network (seed regime of bench/common/mod.rs). The `cull/noise_sigma__*` kernel carries the ~22 MB transient-allocation hotspot noted in the decision doc §9."
    },
    {
      "id": "cull/noise_sigma__1024",
      "budget_ns": 7462338,
      "tolerance_ratio": 1.2,
      "gate": false,
      "note": "New assisted-culling class (LRPAR-G09-CULL-25 / F-074), measured with `cargo bench -p lumina-bench --bench cull`. Captured 2026-09-17 on the capture machine (rustc 1.98.0) against baseline median_ns = 3731169 ns, budget_ns = 2x. gate:false (report-only) until the class is independently calibrated (F-074 methodology: new classes start in report mode, like the GPU/Merge classes). Deterministic, model-free Stage-1 heuristic, no ONNX/weights, no network (seed regime of bench/common/mod.rs). The `cull/noise_sigma__*` kernel carries the ~22 MB transient-allocation hotspot noted in the decision doc §9."
    },
    {
      "id": "cull/noise_sigma__2048",
      "budget_ns": 30041162,
      "tolerance_ratio": 1.2,
      "gate": false,
      "note": "New assisted-culling class (LRPAR-G09-CULL-25 / F-074), measured with `cargo bench -p lumina-bench --bench cull`. Captured 2026-09-17 on the capture machine (rustc 1.98.0) against baseline median_ns = 15020581 ns, budget_ns = 2x. gate:false (report-only) until the class is independently calibrated (F-074 methodology: new classes start in report mode, like the GPU/Merge classes). Deterministic, model-free Stage-1 heuristic, no ONNX/weights, no network (seed regime of bench/common/mod.rs). The `cull/noise_sigma__*` kernel carries the ~22 MB transient-allocation hotspot noted in the decision doc §9."
    },
    {
      "id": "cull/similarity_signature__512",
      "budget_ns": 773600,
      "tolerance_ratio": 1.2,
      "gate": false,
      "note": "New assisted-culling class (LRPAR-G09-CULL-25 / F-074), measured with `cargo bench -p lumina-bench --bench cull`. Captured 2026-09-17 on the capture machine (rustc 1.98.0) against baseline median_ns = 386800 ns, budget_ns = 2x. gate:false (report-only) until the class is independently calibrated (F-074 methodology: new classes start in report mode, like the GPU/Merge classes). Deterministic, model-free Stage-1 heuristic, no ONNX/weights, no network (seed regime of bench/common/mod.rs). The `cull/noise_sigma__*` kernel carries the ~22 MB transient-allocation hotspot noted in the decision doc §9."
    },
    {
      "id": "cull/similarity_signature__1024",
      "budget_ns": 3158398,
      "tolerance_ratio": 1.2,
      "gate": false,
      "note": "New assisted-culling class (LRPAR-G09-CULL-25 / F-074), measured with `cargo bench -p lumina-bench --bench cull`. Captured 2026-09-17 on the capture machine (rustc 1.98.0) against baseline median_ns = 1579199 ns, budget_ns = 2x. gate:false (report-only) until the class is independently calibrated (F-074 methodology: new classes start in report mode, like the GPU/Merge classes). Deterministic, model-free Stage-1 heuristic, no ONNX/weights, no network (seed regime of bench/common/mod.rs). The `cull/noise_sigma__*` kernel carries the ~22 MB transient-allocation hotspot noted in the decision doc §9."
    },
    {
      "id": "cull/similarity_signature__2048",
      "budget_ns": 12806970,
      "tolerance_ratio": 1.2,
      "gate": false,
      "note": "New assisted-culling class (LRPAR-G09-CULL-25 / F-074), measured with `cargo bench -p lumina-bench --bench cull`. Captured 2026-09-17 on the capture machine (rustc 1.98.0) against baseline median_ns = 6403485 ns, budget_ns = 2x. gate:false (report-only) until the class is independently calibrated (F-074 methodology: new classes start in report mode, like the GPU/Merge classes). Deterministic, model-free Stage-1 heuristic, no ONNX/weights, no network (seed regime of bench/common/mod.rs). The `cull/noise_sigma__*` kernel carries the ~22 MB transient-allocation hotspot noted in the decision doc §9."
    },
    {
      "id": "cull/analyze_selection__4x512",
      "budget_ns": 16584276,
      "tolerance_ratio": 1.2,
      "gate": false,
      "note": "New assisted-culling class (LRPAR-G09-CULL-25 / F-074), measured with `cargo bench -p lumina-bench --bench cull`. Captured 2026-09-17 on the capture machine (rustc 1.98.0) against baseline median_ns = 8292138 ns, budget_ns = 2x. gate:false (report-only) until the class is independently calibrated (F-074 methodology: new classes start in report mode, like the GPU/Merge classes). Deterministic, model-free Stage-1 heuristic, no ONNX/weights, no network (seed regime of bench/common/mod.rs). The `cull/noise_sigma__*` kernel carries the ~22 MB transient-allocation hotspot noted in the decision doc §9."
    },
    {
      "id": "cull/status_evaluate__valid",
      "budget_ns": 288,
      "tolerance_ratio": 1.2,
      "gate": false,
      "note": "New assisted-culling class (LRPAR-G09-CULL-25 / F-074), measured with `cargo bench -p lumina-bench --bench cull`. Captured 2026-09-17 on the capture machine (rustc 1.98.0) against baseline median_ns = 144 ns, budget_ns = 2x. gate:false (report-only) until the class is independently calibrated (F-074 methodology: new classes start in report mode, like the GPU/Merge classes). Deterministic, model-free Stage-1 heuristic, no ONNX/weights, no network (seed regime of bench/common/mod.rs). The `cull/noise_sigma__*` kernel carries the ~22 MB transient-allocation hotspot noted in the decision doc §9."
    }
  ]
}
